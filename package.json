{
  "name": "cropdr-fullstack",
  "version": "1.0.0",
  "description": "Root package.json for easy starting and deployment",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "install:backend": "npm install --prefix backend"
  }
}
